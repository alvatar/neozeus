# TODO — HUD retained architecture + dispatcher + AgentList

Source plan: `work/PLAN-20260315-150053.md`

## Phase 1 — Remove old HUD ownership and split subsystem

### 1.1 Restructure files
- [x] Convert `src/hud.rs` into `src/hud/`
- [x] Add files:
  - [x] `src/hud/mod.rs`
  - [x] `src/hud/state.rs`
  - [x] `src/hud/messages.rs`
  - [x] `src/hud/dispatcher.rs`
  - [x] `src/hud/persistence.rs`
  - [x] `src/hud/input.rs`
  - [x] `src/hud/animation.rs`
  - [x] `src/hud/render.rs`
  - [x] `src/hud/modules/mod.rs`
  - [x] `src/hud/modules/debug_toolbar.rs`
  - [x] `src/hud/modules/agent_list.rs`
- [x] Update `src/main.rs` module declaration if needed

### 1.2 Remove obsolete HUD ownership
- [x] Delete old EVA demo visuals from HUD code
- [x] Remove egui toolbar ownership from current HUD path
- [x] Preserve only reusable Vello integration and app controls that will be moved into `DebugToolbar`

### 1.3 Rewire scene imports
- [x] Update `src/scene.rs` imports to use `hud::...` submodule API
- [x] Keep compile surface narrow in `src/hud/mod.rs`

### Phase 1 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

---

## Phase 2 — Retained HUD state + module registry

### 2.1 Core geometry/state types
- [x] Add `HudRect`
- [x] Add `HudModuleId`
  - [x] `DebugToolbar`
  - [x] `AgentList`
  - [x] explicit `number()` mapping (`0`, `1`)
  - [x] explicit `title()` mapping
- [x] Add `HudModuleShell`
- [x] Add `HudModuleInstance`
- [x] Add `HudState` resource
- [x] Add `HudDragState`

### 2.2 Module-local state
- [x] Add `DebugToolbarState`
- [x] Add `AgentListState`
- [x] Add `HudModuleModel` enum

### 2.3 Static definitions
- [x] Add `HudModuleDefinition`
- [x] Add module definition registry/helper list
- [x] Define default rects for:
  - [x] `DebugToolbar`
  - [x] `AgentList`

### 2.4 Agent metadata
- [x] Add `AgentDirectory` resource
- [x] Add helper to resolve display label from:
  - [x] explicit directory label
  - [x] fallback `agent-{n}`

### 2.5 HUD bootstrap
- [x] Add HUD initialization helper to create default module instances
- [x] Insert `HudState`
- [x] Insert `AgentDirectory`

### Phase 2 gate
- [x] Build compiles with empty/non-rendered HUD state
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

---

## Phase 3 — Dispatcher and message types

### 3.1 Message definitions
- [x] Add `HudCommand`
  - [x] `FocusTerminal(TerminalId)`
  - [x] `HideAllButTerminal(TerminalId)`
  - [x] `RenameAgent { terminal_id, label }`
  - [x] `SetModuleEnabled { id, enabled }`
  - [x] `ToggleModule(HudModuleId)`
- [x] Add `HudEvent`
  - [x] `TerminalFocused(TerminalId)`
  - [x] `TerminalPresentationIsolated(TerminalId)`
  - [x] `TerminalSpawned(TerminalId)`
  - [x] `TerminalClosed(TerminalId)`
  - [x] `AgentRenamed { terminal_id, label }`
- [x] Add `HudRecipients`
- [x] Add `HudEnvelope<T>`

### 3.2 Dispatcher resource
- [x] Add `HudDispatcher` resource
- [x] Add queue-drain helpers if useful
- [x] Decide whether helpers live in `dispatcher.rs` or `messages.rs`

### 3.3 Module event update shape
- [x] Add module update entrypoint signature for event delivery
- [x] Keep enum-based dispatch, not trait objects

### Phase 3 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

---

## Phase 4 — HUD layout persistence

### 4.1 Persisted types
- [x] Add `PersistedHudModuleState`
- [x] Add `PersistedHudState`

### 4.2 Path resolution
- [x] Implement config path resolution:
  - [x] default `${XDG_CONFIG_HOME}/neozeus/hud-layout.v1`
  - [x] fallback `${HOME}/.config/neozeus/hud-layout.v1`
- [x] Ensure parent directory creation is handled safely

### 4.3 Parser/writer
- [x] Implement line-based parser
- [x] Implement line-based writer
- [x] Handle missing file as defaults
- [x] Handle malformed file as log + defaults

### 4.4 Runtime integration
- [x] Load persisted layout at startup
- [x] Merge missing modules with defaults
- [x] Mark `HudState.dirty_layout` on shell state change
- [x] Add debounced save system/resource state

### 4.5 Tests
- [x] Path resolution test(s) if practical
- [x] Parse/write roundtrip test(s)
- [x] Malformed file fallback test(s)

### Phase 4 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

---

## Phase 5 — HUD input + drag controller

### 5.1 Pointer state and hit testing
- [x] Add cursor tracking helpers in HUD space
- [x] Add shell hit test helpers:
  - [x] module bounds
  - [x] titlebar bounds
  - [x] content bounds
- [x] Add top-most module selection by z-order

### 5.2 Drag controller
- [x] Pointer-down on titlebar starts drag
- [x] Raise dragged module to front
- [x] Pointer-move updates `target_rect`
- [x] Pointer-up clears drag state
- [x] Mark layout dirty on rect updates

### 5.3 Content input routing
- [x] Define content input API for modules
- [x] Route pointer clicks to module content handlers
- [x] Route wheel scrolling to module content handlers

### 5.4 Redraw hook-up
- [x] Ensure dragging marks HUD as visually active
- [x] Ensure reactive redraw occurs during drag

### 5.5 Validation
- [x] Drag works on empty shell frame before module content exists

### Phase 5 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`
- [x] Runtime smoke with visible drag

---

## Phase 6 — HUD animation state

### 6.1 Animation system
- [x] Add shell animation tick system
- [x] Interpolate:
  - [x] rect position
  - [x] rect size
  - [x] alpha
- [x] Keep target rect authoritative
- [x] Keep persistence using target rect only

### 6.2 Optional shell states
- [x] Define open/close alpha policy even if modules start always enabled
- [x] Decide whether z-order affects alpha or only draw order

### 6.3 Redraw integration
- [x] HUD animation contributes to redraw demand

### Phase 6 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`
- [x] Runtime smoke with animated drag settle

---

## Phase 7 — Vello HUD renderer + chrome helpers

### 7.1 HUD Vello scene plumbing
- [x] Add `HudVectorSceneMarker`
- [x] Spawn a dedicated HUD Vello scene entity in scene setup
- [x] Ensure draw order is above terminals

### 7.2 Shared chrome helpers
- [x] Add frame drawing helper
- [x] Add titlebar drawing helper
- [x] Add label/text drawing helper
- [x] Add clipping/content rect helper policy
- [x] Add theme constants for padding/titlebar sizes/colors

### 7.3 Renderer system
- [x] Add system to rebuild HUD Vello scene from `HudState`
- [x] Iterate modules in z-order
- [x] Skip disabled modules
- [x] Render shell first, then content

### 7.4 Remove obsolete visuals
- [x] Remove EVA demo scene generation from active app
- [x] Remove any leftover demo-specific state/resources/systems not needed for HUD framework

### Phase 7 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`
- [x] Runtime smoke: visible HUD shell frame renders in Vello

---

## Phase 8 — DebugToolbar module

### 8.1 Module definition
- [x] Register `DebugToolbar` definition
- [x] Default enabled
- [x] Default rect at top of screen

### 8.2 Minimal state + rendering
- [x] Add `DebugToolbarState`
- [x] Render toolbar shell/title/content
- [x] Port critical controls from old egui toolbar:
  - [x] active terminal summary
  - [x] terminal count / active id
  - [x] pixel perfect toggle if still needed during transition
  - [x] reset view
  - [x] terminal command buttons if still needed (`pwd`, `ls`, `clear`, `btop`, `tmux`)
  - [x] optional module toggle summary

### 8.3 Input handling
- [x] Toolbar buttons emit `HudCommand`s or direct safe local toggles where appropriate
- [x] Keep `DebugToolbar` as a normal HUD module, not a special case

### 8.4 Remove old egui toolbar
- [x] Delete old `ui_overlay` egui toolbar code once parity is reached
- [x] Remove `bevy_egui` from active HUD ownership paths
- [x] Decide whether `bevy_egui` dependency stays temporarily or can be removed later

### Phase 8 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`
- [x] Runtime smoke: toolbar controls still operate app

---

## Phase 9 — AgentList module

### 9.1 Module definition and state
- [x] Register `AgentList` definition
- [x] Add `AgentListState { scroll_offset }`
- [x] Default enabled and default rect

### 9.2 Data model view helpers
- [x] Add helper to derive agent rows from `TerminalManager`
- [x] Add helper to resolve display labels from `AgentDirectory` + fallback naming
- [x] Add helper to identify focused row from `terminal_manager.active_id()`

### 9.3 Rendering
- [x] Render content area rows vertically
- [x] Render row hover state
- [x] Render row focused state
- [x] Add clipping for scrolled list if needed

### 9.4 Input
- [x] Click row emits:
  - [x] `HudCommand::FocusTerminal(id)`
  - [x] `HudCommand::HideAllButTerminal(id)`
- [x] Wheel scroll mutates `scroll_offset`
- [x] Dragging shell must still work independently of row clicks

### 9.5 Validation
- [x] Multiple terminals appear as `agent-1`, `agent-2`, ...
- [x] Clicking a row focuses corresponding terminal
- [x] Clicking a row triggers hide/minimize presentation policy for others

### Phase 9 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`
- [x] Runtime smoke with at least two terminals

---

## Phase 10 — Command application, events, presentation policy

### 10.1 Presentation policy resource
- [x] Add `TerminalVisibilityPolicy`
  - [x] `ShowAll`
  - [x] `Isolate(TerminalId)`

### 10.2 Command application system
- [x] Drain `HudDispatcher.commands`
- [x] Apply:
  - [x] `FocusTerminal`
  - [x] `HideAllButTerminal`
  - [x] `RenameAgent`
  - [x] `SetModuleEnabled`
  - [x] `ToggleModule`
- [x] Emit matching `HudEvent`s

### 10.3 Event dispatch system
- [x] Drain `HudDispatcher.events`
- [x] Deliver to one / some / all modules
- [x] Add no-op handling where modules ignore irrelevant events

### 10.4 Integrate presentation policy into terminal presentation systems
- [x] Read `TerminalVisibilityPolicy` from presentation layer
- [x] Hide/minimize/de-emphasize non-selected terminals without deleting them
- [x] Keep runtime state untouched
- [x] Keep focus and visibility semantics separate

### 10.5 Rename path
- [x] Apply `RenameAgent` into `AgentDirectory`
- [x] Broadcast `AgentRenamed`
- [x] Ensure `AgentList` reflects renamed label automatically

### Phase 10 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`
- [x] Runtime smoke: hide/minimize is presentation-only, terminal stays alive

---

## Phase 11 — Redraw integration and cleanup

### 11.1 Schedule integration
- [x] Add explicit HUD system sets:
  - [x] `HudInput`
  - [x] `HudCommands`
  - [x] `HudEvents`
  - [x] `HudAnimation`
  - [x] `HudRender`
- [x] Order them before redraw scheduling

### 11.2 Redraw demand
- [x] HUD drag contributes to redraw demand
- [x] HUD animation contributes to redraw demand
- [x] Dirty layout/save cooldown contributes if it affects visuals
- [x] Renderer scene rebuild contributes to redraw demand

### 11.3 Cleanup
- [x] Remove dead EVA demo state/resources/systems
- [x] Remove dead egui toolbar code
- [x] Reduce stale imports/reexports introduced by the HUD split

### 11.4 Manual validation checklist
- [x] app boots cleanly
- [x] `DebugToolbar` visible and draggable
- [x] `AgentList` visible and draggable
- [x] module rect persists across restart
- [x] clicking `AgentList` row focuses terminal
- [x] clicking `AgentList` row hides/minimizes others only in presentation
- [x] hidden terminal still exists and can be shown again
- [x] typing still repaints terminal under reactive mode
- [x] terminal command buttons still work if retained in `DebugToolbar`

### 11.5 Final static gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

Validation coverage for the remaining runtime interaction items is now split across:
- deterministic Rust tests in `src/tests.rs` for drag/input/animation/persistence/presentation-policy behavior
- existing GUI verifiers for visible terminal output and terminal color correctness

---

## Suggested execution order inside implementation

1. Phase 1
2. Phase 2
3. Phase 3
4. Phase 4
5. Phase 5
6. Phase 6
7. Phase 7
8. Phase 8
9. Phase 9
10. Phase 10
11. Phase 11

---

## Notes for implementation

- Keep module state retained; never store truth in Vello scene objects
- Keep focus separate from visibility policy
- Keep `AgentList` as a view over terminal/domain state, not its own authority
- Keep `DebugToolbar` temporary in scope, but architecturally first-class
- Avoid new dependencies
- Commit after each major phase or meaningful completed chunk
