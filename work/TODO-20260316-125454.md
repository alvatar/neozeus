# TODO — tmux-backed terminal persistence + startup recovery + explicit kill

Source plan: `work/PLAN-20260316-123319.md`

## Phase 1 — Durable terminal session metadata

### 1.1 New persistence module
- [x] Add `src/terminals/session_persistence.rs`
- [x] Export terminal session persistence API from `src/terminals.rs`

### 1.2 Persisted types
- [x] Add `TerminalSessionRecord`
  - [x] `session_name: String`
  - [x] `label: Option<String>`
  - [x] `creation_index: u64`
  - [x] `last_focused: bool`
- [x] Add `PersistedTerminalSessions`
- [x] Add `TerminalSessionPersistenceState`

### 1.3 Path resolution
- [x] Implement terminal metadata path resolution helper
  - [x] prefer `${XDG_STATE_HOME}/neozeus/terminals.v1`
  - [x] fallback `${HOME}/.local/state/neozeus/terminals.v1`
  - [x] decide/configure config-dir fallback policy explicitly
- [x] Ensure parent directory creation is handled safely

### 1.4 Parser / writer
- [x] Implement line-based parser
- [x] Implement line-based serializer
- [x] Add explicit version line handling
- [x] Treat missing file as empty/default state
- [x] Treat malformed file as log + default state

### 1.5 Tests
- [x] Path resolution tests
- [x] Parse/serialize roundtrip tests
- [x] Malformed file fallback tests

### Phase 1 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

---

## Phase 2 — tmux integration surface + attach target abstraction

### 2.1 Attach/provision target types
- [x] Add `TerminalAttachTarget`
  - [x] `RawShell`
  - [x] `TmuxAttach { session_name }`
- [x] Add `TerminalProvisionTarget`
  - [x] `RawShell`
  - [x] `TmuxDetached { session_name }`

### 2.2 tmux helper module
- [x] Add `src/terminals/tmux.rs`
- [x] Add helper/API for:
  - [x] `ensure_tmux_available()`
  - [x] `create_detached_session(name)`
  - [x] `list_sessions()`
  - [x] `session_exists(name)`
  - [x] `kill_session(name)`
- [x] Keep command execution direct; do not shell-wrap tmux subcommands

### 2.3 Testability boundary
- [x] Decide and implement tmux invocation abstraction
  - [x] concrete runtime impl
  - [x] fake/test impl or command-runner shim
- [x] Ensure deterministic Rust tests do not depend on host tmux

### 2.4 Tests
- [x] tmux attach command argv test(s)
- [x] detached session create failure propagation test(s)
- [x] kill-session failure propagation test(s)
- [x] list/has/kill fake-client tests

### Phase 2 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

---

## Phase 3 — Parameterize PTY spawn by attachment target

### 3.1 Backend refactor
- [x] Refactor `backend::spawn_pty()` to accept `TerminalAttachTarget`
- [x] Add helper to build `CommandBuilder` from attach target
- [x] Preserve existing raw-shell path behavior
- [x] Add tmux attach path:
  - [x] `tmux attach-session -t <session_name>`

### 3.2 Runtime spawner refactor
- [x] Extend `TerminalRuntimeSpawner` with explicit spawn-attached API
- [x] Keep notifier/bridge/mailbox semantics unchanged where possible
- [x] Ensure attach failure surfaces as runtime failure status

### 3.3 Tests
- [x] attach target -> command builder tests
- [x] raw-shell path regression test(s) if practical
- [x] runtime failure-status path test(s) for attach failure

### Phase 3 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

---

## Phase 4 — Session identity in runtime/domain state

### 4.1 Manager state extension
- [x] Add `session_name: String` to `ManagedTerminal`
- [x] Add terminal creation helpers that accept/preserve session name
  - [x] focused create + slot
  - [x] unfocused create + slot
- [x] Keep creation-order vs focus-order invariant intact

### 4.2 Removal lifecycle foundation
- [x] Add `TerminalManager::remove_terminal(id)`
- [x] Ensure removal updates:
  - [x] `active_id`
  - [x] `creation_order`
  - [x] `focus_order`
  - [x] terminal map
- [x] Add `TerminalPresentationStore::remove(id)`

### 4.3 ECS cleanup support
- [x] Add scene/presentation cleanup path for removed terminal IDs
- [x] Remove both `TerminalPanel` and `TerminalPanelFrame` entities
- [x] Ensure no stale hidden entities remain after removal

### 4.4 Tests
- [x] session name retained in manager state
- [x] create-without-focus retains session name and leaves `active_id == None`
- [x] remove-terminal clears all manager indices/orders correctly
- [x] remove-terminal cleanup removes presentation store entry

### Phase 4 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

---

## Phase 5 — New terminal provisioning via tmux

### 5.1 Session naming
- [x] Add session-name generator independent of `TerminalId`
- [x] Prefix with `neozeus-`
- [x] Make collision-safe via `tmux has-session` / retry

### 5.2 Detached provision + attach flow
- [x] Provision detached tmux session before logical terminal creation
- [x] Attach neozeus PTY to that session
- [x] On success, create logical terminal with session name and spawn presentation
- [x] On attach failure after detached create:
  - [x] log clearly
  - [x] keep orphan session for later import/recovery
  - [x] avoid inserting inconsistent local terminal state

### 5.3 Spawn call sites
- [x] Update HUD/debug-toolbar spawn path to use tmux-backed provisioning
- [x] Update plain-`z` bootstrap path to use tmux-backed provisioning
- [x] Preserve verifier-mode deterministic startup path explicitly

### 5.4 Tests
- [x] bootstrap path still only accepts plain `z`
- [x] bootstrap creates tmux-backed unfocused terminal when terminal set is empty
- [x] spawn path associates session name with created terminal
- [x] failure path does not leave partial local terminal state

### Phase 5 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

---

## Phase 6 — Explicit active-terminal kill semantics (`Ctrl-K`)

### 6.1 Product semantics
- [x] Implement `Ctrl-K` for active terminal only
- [x] No active terminal => no-op
- [x] Keep destructive operation centralized, not ad-hoc in input handling

### 6.2 Command/action plumbing
- [x] Decide routing shape
  - [x] extend existing command path with `KillActiveTerminal`, or
  - [x] add terminal-action dispatcher with equivalent centralization
- [x] Route `Ctrl-K` shortcut into that path
- [x] Ensure existing `Ctrl-K` terminal input behavior is intentionally overridden or gated by app policy

### 6.3 Kill implementation
- [x] Resolve active terminal from `TerminalManager`
- [x] Read `session_name`
- [x] Call `tmux kill-session -t <session_name>`
- [x] If tmux kill succeeds:
  - [x] remove runtime/domain state
  - [x] remove presentation store entry
  - [x] despawn terminal panel/frame entities
  - [x] remove label metadata
  - [x] mark session persistence dirty
  - [x] leave no active terminal for first implementation
- [x] If tmux kill fails:
  - [x] keep terminal alive
  - [x] log/report failure clearly

### 6.4 Tests
- [x] `Ctrl-K` no-op when no active terminal exists
- [x] successful kill removes all local state for active terminal
- [x] kill failure preserves local state
- [x] removal leaves no stale `active_id` / `focus_order` / `creation_order` / label / presentation refs

### Phase 6 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

---

## Phase 7 — Persist terminal metadata from live runtime state

### 7.1 Save triggers
- [x] Mark session persistence dirty on:
  - [x] new terminal created
  - [x] terminal focused
  - [x] terminal killed/removed
  - [x] label changed

### 7.2 Save system
- [x] Add `save_terminal_sessions_if_dirty(...)`
- [x] Use debounce behavior similar to HUD persistence if still justified
- [x] Persist from live resources, not cached records
  - [x] `TerminalManager` for order/focus/session name
  - [x] `AgentDirectory` for labels

### 7.3 Persisted semantics
- [x] Persist creation order from `TerminalManager.creation_order`
- [x] Persist active/focused session from `active_id`
- [x] Exclude transient attachment/runtime transport state

### 7.4 Tests
- [x] save roundtrip from live resources
- [x] focus change updates persisted focus
- [x] removal updates persisted set/order correctly
- [x] save failure is non-fatal

### Phase 7 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

---

## Phase 8 — Startup recovery / reconcile

### 8.1 Recovery input sources
- [x] Load persisted terminal metadata in startup path
- [x] List live tmux sessions
- [x] Filter recoverable/importable sessions to `neozeus-*`

### 8.2 Reconcile algorithm
- [x] Compute categories:
  - [x] restore = persisted + live
  - [x] prune = persisted only
  - [x] import = live `neozeus-*` only
- [x] Define deterministic restore/import ordering
- [x] Log pruned stale metadata records

### 8.3 Startup recreation
- [x] For restored/imported sessions:
  - [x] attach runtime to existing tmux session
  - [x] create logical terminal without implicit focus
  - [x] spawn presentation
  - [x] restore label if persisted
- [x] Restore focused session only if still live
- [x] If persisted focused session is missing, start with no active terminal

### 8.4 Verifier precedence
- [x] Decide and implement verifier precedence explicitly
  - [x] recommended: verifier mode bypasses recovery/import and spawns its own deterministic terminal
- [x] Ensure verifier path does not duplicate recovered sessions

### 8.5 Tests
- [x] reconcile restore/prune/import pure tests
- [x] startup restore ordering test(s)
- [x] focus restoration test(s)
- [x] stale metadata prune test(s)
- [x] verifier precedence test(s) if practical

### Phase 8 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

---

## Phase 9 — Preserve and verify UX semantics

### 9.1 Startup / bootstrap semantics
- [x] Normal startup remains empty when nothing is recoverable
- [x] plain `z` only bootstraps first hidden terminal when terminal count is zero
- [x] if sessions were restored/imported, `z` bootstrap no longer triggers

### 9.2 Visibility / focus semantics
- [x] hidden/unfocused terminal remains hidden while `active_id == None`
- [x] existing focus path still brings terminal foreground
- [x] `Ctrl-K` leaves no active terminal for first implementation

### 9.3 Error handling semantics
- [x] tmux missing on host => graceful degradation, no panic
- [x] attach/provision/kill failures => explicit logs/status, no silent corruption
- [x] metadata corruption => log + continue startup

### 9.4 Tests
- [x] hidden presentation when no terminal active still holds after recovery integration
- [x] restored sessions do not duplicate local state
- [x] tmux-missing path degrades gracefully in tested code path(s)

### Phase 9 gate
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

---

## Phase 10 — Final verification

### 10.1 Static / deterministic validation
- [x] `cargo test`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo fmt --check`

### 10.2 GUI validation
- [ ] `./scripts/gui/run-suite.sh`
- [ ] Verify verifier-mode startup still works deterministically
- [ ] Verify startup recovery path does not interfere with GUI verification mode

Note:
- current environment is blocking `grim` globally with `no supported format found`; deterministic Rust/static validation passed, but GUI capture-based validation is currently externally blocked

### 10.3 Manual crash-recovery validation
- [ ] Start neozeus, create tmux-backed terminal(s)
- [ ] Kill neozeus process without killing tmux server
- [ ] Restart neozeus
- [ ] Confirm sessions are rediscovered and restored
- [ ] Confirm labels/order/focus behavior matches plan
- [ ] Confirm `Ctrl-K` kills active tmux session and removes it locally

Note:
- seeded recovery path was runtime-checked (persisted metadata + live tmux session -> app restored active session), but a full compositor-driven end-to-end manual workflow remains to be exercised

---

## Cross-cutting invariants

- [x] tmux remains process authority; neozeus metadata remains UI/domain authority
- [x] no persisted durable identity depends on process-local `TerminalId`
- [x] PTY render/input pipeline above attach target remains unchanged
- [x] creation order remains stable across focus changes
- [x] removal leaves no stale runtime/store/entity metadata
- [x] no new external dependency added; use system `tmux`
- [x] failure paths are explicit and non-fatal
