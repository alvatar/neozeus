# TODO — fix all issues found during research (rolled forward)

Source plan:
- `work/PLAN-20260410-120732.md`

Source research:
- `work/RESEARCH-20260410-105557.md`
- `work/RESEARCH-20260410-220940.md`

Current implemented progress already on `master`:
- reset preserves persistence paths and clears notes correctly
- app-state persistence now includes runtime binding / focus / clone provenance / Aegis fields
- live-only missing-runtime restore is reported during reset
- daemon stale-socket handling is conservative
- shared atomic write + shared path helpers exist
- focus authority/projection comments were added

Execution rules:
- complete phases in order unless explicitly approved otherwise
- no behavior-changing refactor without regression coverage first
- correctness + reporting truth first, architecture cleanup second, decomposition last
- keep commit boundaries reviewable; do not mix large refactors with semantic changes

---

## Phase 0 — Close the missing regression coverage

### 0.1 Reset/persistence follow-up tests
- [x] Add test: reset followed by conversation mutation still persists conversations
- [x] Add test: reset followed by notes/task mutation still persists notes
- [x] Add test: reset + restore preserves expected app-state save behavior
- [x] Add test: reset remains idempotent when triggered twice
- [x] Add test: partial daemon kill failure during reset does not corrupt local state

### 0.2 App-state restore regressions still missing
- [x] Add roundtrip test: load+restore reattaches by `runtime_session_name` when still live
- [x] Add roundtrip test: restore falls back to `recovery` when runtime session is gone
- [x] Add roundtrip test: imported live sessions serialize their runtime session binding correctly
- [x] Add roundtrip test: owned tmux focus does not incorrectly mark an unrelated agent focused
- [x] Add roundtrip test: Pi clone provenance roundtrips independently of recovery fallback

### 0.3 Focus/selection regressions still missing
- [x] Add test: focusing agent updates all dependent projections coherently
- [x] Add test: clearing focus clears all dependent projections coherently
- [x] Add test: owned tmux selection projects consistently into focus/selection state
- [x] Add test: direct terminal input reconciles with projected active terminal
- [x] Add test: terminal removal prunes focus/selection without drift

### 0.4 Live-only / recoverable regressions still missing
- [x] Add test: recoverable agents restore after daemon/runtime loss
- [x] Add test: live-only agents are reattached if still live
- [x] Add test: imported live-only agents retain stable label/UID when reattached

### 0.5 New review regressions
- [x] Add test: startup recovery status includes skipped live-only agents in title/details
- [x] Add test: reset recovery status and startup recovery status render the same summary semantics
- [x] Add test: app-state parser handles EOF-truncated final `[agent]` block according to chosen policy
- [x] Add test: shell quoting helper behavior is identical across runtime/tmux/helper CLI callers
- [x] Add test: Linux display/backend inference helper gives the same answer to bootstrap and clipboard callers

### 0.6 Verification gates
- [x] Run targeted tests for reset/persistence/startup/focus/runtime modules
- [x] Run full `cargo test`

---

## Phase 1 — Finish reset correctness and restore coherence

### 1.1 Remaining reset correctness audit
- [x] Audit every persistence resource touched by reset/recovery for accidental `Default` replacement
- [x] Confirm `AppStatePersistenceState` dirty handling stays correct after reset-driven restore
- [x] Confirm reset clears all stale per-agent task/conversation/note projections, not just primary stores

### 1.2 Rebuild projections after restore
- [x] Ensure restore path reruns task→notes projection after runtime rebuild
- [x] Ensure focus projection still runs after reset-driven restore
- [x] Ensure conversation/task/note projections are internally coherent after reset+restore

### 1.3 Reset reporting behavior
- [x] Confirm reset still reports recovery details correctly for restored / failed / skipped cases
- [x] Confirm reset reporting remains correct when snapshot is absent
- [x] Confirm reset reporting remains correct when daemon discovery fails

### 1.4 Verification gates
- [x] Run reset/recovery/startup tests
- [x] Run full `cargo test`

---

## Phase 2 — Centralize recovery status rendering and fix startup reporting drift

### 2.1 Introduce one rendering path for `RecoveryExecutionSummary`
- [ ] Add a small helper/value object that maps `RecoveryExecutionSummary` + mode (`Startup` / `Reset`) to:
  - title
  - tone
  - detail lines
- [ ] Remove duplicated summary formatting logic from `startup.rs`
- [ ] Remove duplicated summary formatting logic from `recovery.rs`

### 2.2 Fix the known startup drift bug
- [ ] Include `skipped_agents` in startup recovery title
- [ ] Include `skipped_agents` in startup recovery details
- [ ] Ensure startup and reset use the same counting semantics for restored / failed / skipped

### 2.3 Make reporting policy explicit
- [ ] Decide whether “snapshot found but daemon discovery failed” is rendered as failed recovery vs degraded startup
- [ ] Decide whether “no snapshot” should always clear status or show an informational success state
- [ ] Keep wording concise and consistent between reset/startup paths

### 2.4 Verification gates
- [ ] Run startup/reset scene tests
- [ ] Run full `cargo test`

---

## Phase 3 — Finish app-state persistence truthfulness

### 3.1 Remaining save/restore alignment
- [ ] Ensure imported live sessions serialize runtime binding correctly
- [ ] Confirm absent runtime session remains valid for recoverable records
- [ ] Confirm owned tmux focus never leaks into unrelated agent focus persistence

### 3.2 Restore behavior alignment cleanup
- [ ] Audit `restore_app()` for any remaining branches not using richer persisted fields consistently
- [ ] Confirm helper CLI app-state readers still work with the updated serialized form
- [ ] Remove comments/docs describing omitted fields as intended behavior

### 3.3 Verification gates
- [ ] Run app persistence tests
- [ ] Run startup/restore tests
- [ ] Run helper CLI tests touching app-state parsing
- [ ] Run full `cargo test`

---

## Phase 4 — Make durable vs live-only semantics explicit

### 4.1 Introduce explicit durability model
- [ ] Add `AgentDurability` (or equivalent) to the domain model
- [ ] Decide whether durability is stored in persisted app-state, derived, or both
- [ ] Define legacy default behavior when old records lack durability

### 4.2 Classify all agent creation/import paths
- [ ] Pi / Claude / Codex with recovery => recoverable
- [ ] raw `Terminal` without recovery => live-only
- [ ] verifier => live-only unless a stronger reason appears
- [ ] imported daemon sessions without recovery => live-only unless evidence says otherwise

### 4.3 Align restore/reconcile behavior
- [ ] If live-only session still exists: reattach/import
- [ ] If recoverable session missing: respawn via recovery spec
- [ ] If live-only session missing: skip respawn and report explicitly
- [ ] Ensure imported live-only agents retain stable label/UID semantics where possible

### 4.4 Verification gates
- [ ] Run restore/reconcile tests
- [ ] Run startup scene tests
- [ ] Run helper CLI tests
- [ ] Run full `cargo test`

---

## Phase 5 — Collapse focus/selection split-brain in call sites

### 5.1 Projection API cleanup
- [ ] Rename/split `apply_focus_intent()` if needed into “set intent” vs “project intent” responsibilities
- [ ] Add narrow helper(s) that mutate focus intent without directly mutating projections elsewhere
- [ ] Keep one projection fan-out path for all focus-related state

### 5.2 Remove direct writes to derived focus state
- [ ] Audit direct writes to `AgentListSelection`
- [ ] Audit direct writes to `TerminalFocusState`
- [ ] Audit direct writes to `TerminalVisibilityState`
- [ ] Audit direct writes to `TerminalViewState`
- [ ] Audit direct writes to `ActiveTerminalContentState`
- [ ] Convert call sites to mutate focus intent first, then project

### 5.3 Owned tmux integration
- [ ] Ensure owned tmux selection is represented as focus intent, not parallel authority
- [ ] Verify owner-terminal fallback behavior remains intact
- [ ] Verify clearing/removing owned tmux selection clears projections cleanly

### 5.4 Terminal removal/reconciliation path
- [ ] Ensure terminal removal drives focus-intent reconciliation rather than ad hoc projection repair
- [ ] Simplify stale-terminal cleanup paths to use the canonical projection step

### 5.5 Verification gates
- [ ] Run focus/input/HUD tests
- [ ] Run startup/selection/owned tmux tests
- [ ] Run full `cargo test`

---

## Phase 6 — Reduce agent/runtime split-brain

### 6.1 Formalize field ownership
- [ ] Document ownership for:
  - session existence/runtime lifecycle
  - daemon metadata
  - app catalog metadata
  - recovery metadata
  - conversations/tasks/Aegis state
- [ ] Add code comments near daemon metadata sync paths enforcing intended ownership

### 6.2 Centralize daemon metadata synchronization
- [ ] Add helper for pushing app-side agent metadata into daemon session metadata
- [ ] Route create / rename / restore / import flows through that helper
- [ ] Ensure helper handles no-session / disconnected-session cases safely

### 6.3 Simplify `sync_agents_from_terminals()`
- [ ] Split stale-terminal cleanup from import/create logic
- [ ] Keep runtime lifecycle sync local and obvious
- [ ] Remove unrelated cleanup/projection work where possible

### 6.4 Restore/import consistency
- [ ] Ensure imported daemon sessions create app records with stable UID/label/kind semantics
- [ ] Ensure restore uses daemon metadata only for fields daemon truly owns
- [ ] Ensure recovery metadata remains app-only

### 6.5 Verification gates
- [ ] Run agent/runtime_index/startup/daemon-client tests
- [ ] Run helper CLI tests
- [ ] Run full `cargo test`

---

## Phase 7 — Unify daemon protocol definitions

### 7.1 Choose canonical protocol home
- [ ] Decide final canonical module path (recommended: shared wire module)
- [ ] Inventory all duplicated request/response and wire types

### 7.2 Unify shared wire types
- [ ] Move/merge shared `TerminalLifecycle`
- [ ] Move/merge shared `TerminalRuntimeState`
- [ ] Move/merge shared `TerminalCommand`
- [ ] Move/merge shared `DaemonSessionInfo`
- [ ] Move/merge shared `OwnedTmuxSessionInfo`
- [ ] Move/merge client/server message enums

### 7.3 Unify codec implementation
- [ ] Choose one frame encode/decode implementation
- [ ] Remove duplicate tag/codec logic from the non-canonical module
- [ ] Keep helper-binary protocol subsets as thin wrappers only if strictly necessary

### 7.4 Migrate imports incrementally
- [ ] Update daemon server
- [ ] Update daemon client
- [ ] Update helper CLI clients
- [ ] Update tests
- [ ] Delete/reduce duplicate protocol module

### 7.5 Verification gates
- [ ] Run daemon protocol tests
- [ ] Run helper CLI tests
- [ ] Run full `cargo test`

---

## Phase 8 — Normalize persistence grammar/parsing policy

### 8.1 Define common parser policy
- [ ] Decide policy for malformed lines: ignore vs explicit error vs partial recovery
- [ ] Decide policy for truncated final record at EOF
- [ ] Decide policy for unknown fields/unknown sections
- [ ] Decide policy for not-found/corrupt file logging consistency

### 8.2 Harden app-state parser first
- [ ] Split `parse_persisted_app_state_with(...)` into smaller record-level helpers
- [ ] Reduce mutable accumulator count by introducing a record builder / partial record type
- [ ] Handle EOF-final open record according to the chosen policy
- [ ] Add corruption/truncation regression tests

### 8.3 Standardize parser scaffolding across persistence files
- [ ] Align app state / conversations / HUD layout / terminal sessions parser conventions
- [ ] Extract small shared parser helpers only where semantics truly match
- [ ] Avoid over-generic abstractions that hide format behavior

### 8.4 Standardize load/save behavior
- [ ] Add shared dirty-mark helper(s) if worthwhile
- [ ] Add small shared debounce/save helper if it clarifies code
- [ ] Align load-not-found behavior patterns
- [ ] Align save/load failure logging behavior patterns

### 8.5 Verification gates
- [ ] Run persistence tests across all modules
- [ ] Run full `cargo test`

---

## Phase 9 — Introduce shared policy helpers for repeated cross-cutting logic

### 9.1 Shell quoting
- [ ] Introduce one canonical shell-quote helper
- [ ] Migrate runtime launcher to it
- [ ] Migrate daemon-owned tmux code to it
- [ ] Migrate helper CLI quoting to it
- [ ] Verify exact quoting compatibility with current commands

### 9.2 Linux display/backend inference
- [ ] Introduce one helper/value object for session/backend environment inference (`Wayland`/`X11`/unknown, display availability)
- [ ] Reuse it in bootstrap backend forcing
- [ ] Reuse it in primary-selection clipboard code
- [ ] Keep behavior identical unless a deliberate fix is made

### 9.3 Kind conversion helpers
- [ ] Introduce canonical conversions for daemon kind ↔ agent kind ↔ persisted kind
- [ ] Replace repeated open-coded `match` blocks
- [ ] Add regression tests so new agent kinds fail loudly if conversions are incomplete

### 9.4 Verification gates
- [ ] Run launcher/restore/helper CLI tests
- [ ] Run clipboard/input tests
- [ ] Run full `cargo test`

---

## Phase 10 — Refactor startup and verification orchestration boundaries

### 10.1 Startup boundary cleanup
- [ ] Split pure startup decisions from Bevy `World` extraction/orchestration
- [ ] Reduce optional-resource fallback behavior in production paths
- [ ] Replace ad hoc booleans (`restore_started`, `hold_frames_remaining`) with a tighter phase model if practical
- [ ] Keep external behavior unchanged during the refactor

### 10.2 Verification boundary cleanup
- [ ] Split deterministic scenario setup from direct Bevy mutation/orchestration
- [ ] Reduce repeated imperative `focus_agent_without_persist(...)` branches via scenario helpers
- [ ] Remove test-only compatibility hooks from production paths where possible
- [ ] Keep verification scenarios functionally identical

### 10.3 Verification gates
- [ ] Run verification tests
- [ ] Run startup scene tests
- [ ] Run full `cargo test`

---

## Phase 11 — Consolidate the test harness

### 11.1 Extract duplicated test helpers
- [ ] Move duplicated `write_pi_session_file(...)` into shared test support
- [ ] Move duplicated `init_git_repo(...)` into shared test support
- [ ] Centralize common world/bootstrap resource setup helpers where repetition is real
- [ ] Centralize fake runtime spawner helpers where repetition is real

### 11.2 Reduce giant test-file coordination cost
- [ ] Split `src/tests/input.rs` by feature slice if possible
- [ ] Split `src/tests/hud.rs` by feature slice if possible
- [ ] Split `src/tests/scene.rs` by startup/reset/restore slice if possible
- [ ] Keep test names stable enough for targeted execution

### 11.3 Guardrails
- [ ] No behavior changes while extracting test helpers
- [ ] Do not weaken assertions to make refactors easier
- [ ] Preserve targeted-test ergonomics

### 11.4 Verification gates
- [ ] Run touched test modules directly
- [ ] Run full `cargo test`

---

## Phase 12 — Split god-files without semantic changes

### 12.1 Decompose `src/input.rs`
- [ ] Extract shortcut logic
- [ ] Extract terminal pointer logic
- [ ] Extract terminal keyboard/direct-input logic
- [ ] Extract clipboard/primary-selection logic
- [ ] Extract text-selection logic
- [ ] Keep module entry/re-exports stable

### 12.2 Decompose `src/hud/render.rs`
- [ ] Extract painter primitives/helpers
- [ ] Extract dialog rendering
- [ ] Extract text-editor rendering/wrapping helpers
- [ ] Extract module-shell rendering
- [ ] Extract top-level scene render entrypoints

### 12.3 Decompose `src/app/use_cases/spawn_agent_terminal.rs`
- [ ] Extract launch-spec builders
- [ ] Extract provider-specific launch/recovery helpers
- [ ] Extract runtime spawn/attach helpers
- [ ] Extract restore/respawn helpers
- [ ] Extract provider metadata capture helpers

### 12.4 Decompose `src/shared/worktree.rs`
- [ ] Extract git command helpers
- [ ] Extract worktree context/path/branch helpers
- [ ] Extract merge lifecycle helpers
- [ ] Extract create/remove helpers

### 12.5 Guardrails
- [ ] Keep decomposition commits behavior-preserving
- [ ] Avoid mixing refactor and logic changes in the same commit
- [ ] Preserve existing module visibility where possible until cleanup phase

### 12.6 Verification gates
- [ ] Run targeted tests for moved modules
- [ ] Run full `cargo test`

---

## Phase 13 — Reduce giant argument lists with coherent context objects

### 13.1 Identify the worst call surfaces
- [ ] `focus_agent.rs`
- [ ] `spawn_agent_terminal.rs`
- [ ] `restore_app.rs`
- [ ] `recovery.rs`
- [ ] `kill_selected_agent.rs`
- [ ] remaining `too_many_arguments` hotspots with true cohesion

### 13.2 Introduce narrow context structs
- [ ] Add focus projection context
- [ ] Add spawn context
- [ ] Add recovery/reset context
- [ ] Add persistence/save context where useful
- [ ] Prefer `SystemParam` where appropriate inside Bevy systems

### 13.3 Convert call sites incrementally
- [ ] Replace broad arg lists one subsystem at a time
- [ ] Keep APIs explicit; avoid giant “everything context” structs
- [ ] Remove obsolete `clippy::too_many_arguments` suppressions where actually solved

### 13.4 Verification gates
- [ ] Run touched subsystem tests after each conversion batch
- [ ] Run full `cargo test`

---

## Phase 14 — Harden process-global bootstrap policy

### 14.1 Panic-hook policy
- [ ] Re-evaluate temporary global panic-hook swap in `build_app()`
- [ ] Decide whether the current approach remains acceptable or should be isolated behind a smaller startup wrapper
- [ ] Add tests or design notes covering why the chosen approach is safe enough

### 14.2 Environment mutation policy
- [ ] Re-evaluate global env mutation for forced X11 fallback
- [ ] Decide whether policy can be isolated earlier or represented without mutating process-global env late
- [ ] Keep startup behavior stable unless explicitly changing it

### 14.3 Verification gates
- [ ] Run bootstrap/startup tests
- [ ] Run full `cargo test`

---

## Phase 15 — Final verification and cleanup

### 15.1 Formatting/linting
- [ ] Run `cargo fmt --check`
- [ ] Run `cargo fmt` if needed
- [ ] Run `cargo clippy --all-targets --all-features -- -D warnings`
- [ ] Remove obsolete `too_many_arguments` suppressions that are no longer needed

### 15.2 Behavioral verification
- [ ] Verify startup restore path in tests/manual checks
- [ ] Verify reset path in tests/manual checks
- [ ] Verify rename/create/clone/kill flows still work
- [ ] Verify owned tmux create/select/kill flows still work
- [ ] Verify Aegis enable/disable/recovery persistence still works
- [ ] Verify offscreen verification scenarios still boot and capture correctly

### 15.3 Documentation/comments cleanup
- [ ] Remove comments describing old incorrect persistence behavior
- [ ] Ensure doc comments on persistence files match actual behavior
- [ ] Ensure authority comments on focus/agent/runtime ownership boundaries are still accurate

### 15.4 Final pass on risk areas
- [ ] Save/restore roundtrip
- [ ] import vs restore vs respawn interactions
- [ ] helper CLI compatibility with app-state and daemon protocol
- [ ] live-only vs recoverable semantics

---

## Suggested next commit boundaries

### Commit A
- missing regression coverage (Phase 0)

### Commit B
- remaining reset/restore coherence fixes (Phase 1)

### Commit C
- centralized recovery summary rendering + startup drift fix (Phase 2)

### Commit D
- remaining app-state save/restore alignment (Phase 3)

### Commit E
- explicit durability model + restore/reconcile semantics (Phase 4)

### Commit F
- focus/selection authority cleanup (Phase 5)

### Commit G
- agent/runtime ownership + metadata sync cleanup (Phase 6)

### Commit H
- daemon protocol unification (Phase 7)

### Commit I
- persistence grammar normalization + shared helpers (Phases 8–9)

### Commit J
- startup/verification boundary refactor + test harness consolidation (Phases 10–11)

### Commit K
- god-file decomposition + context-object cleanup + bootstrap hardening + final verification (Phases 12–15)

---

## Non-negotiable invariants during execution

- [ ] No tests removed or weakened
- [ ] No new dependency added without approval
- [ ] Legacy persisted files remain readable
- [ ] Daemon wire compatibility preserved unless explicitly versioned
- [ ] Offscreen verification remains isolated/headless-safe
- [ ] Owned tmux workflows remain intact
- [ ] No fake recovery semantics introduced for live-only agents
- [ ] No irreversible migration required for existing saved state
- [ ] Startup and reset must report the same recovery truth for the same underlying summary
